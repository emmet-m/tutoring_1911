#include <stdio.h>
#include <stdlib.h>

int main (void) {
    
    /*
     * Read in up to 100 integers, until no more numbers can be read
     * Ignore all integers that are divisible by 5
     * print the mean, median and mode of the remaining numbers
     */

    return 0;
}
