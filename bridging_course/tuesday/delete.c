/* 
 * delete.c
 *
 * Deletes a given node from a list.
 */

#include <stdio.h>
#include <stdlib.h>

struct node {
    int       data;
    struct node *next;
};

int main (void) {
    
}

struct node * iterative(struct node * head, struct node * toRemove) {
    return NULL;
}

struct node * recursive(struct node * head, struct node * toRemove) {
    return NULL;
}
